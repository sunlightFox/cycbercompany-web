# Browser API root. Keep /api/v1 in development so Vite can proxy same-origin requests.
VITE_API_BASE_URL=/api/v1

# Optional local backend target used by the Vite proxy.
VITE_DEV_PROXY_TARGET=http://localhost:8080
